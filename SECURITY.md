# Security

Do not report this prototype as production-ready. Report suspected
vulnerabilities privately to the repository owner. Never attach generated
contents from `secrets/` or `tokens/`.

The supported demo uses locally generated Ed25519 material and short-lived
bearer tickets. See `docs/threat-model.md` and `docs/security-model.md`.
