// Package identity provides purpose-separated, local identity metadata and
// test-only in-memory signing. It performs no network, protocol-wire, or OS
// keystore operations, and no authority-provider operations.
package identity
