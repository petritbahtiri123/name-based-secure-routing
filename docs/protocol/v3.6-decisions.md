# NBSR Protocol Vision V3.6 decision record

**Status:** Approved architecture direction with unresolved wire and authority
gates

**Date:** 2026-07-28

**Authority:** [NBSR Protocol Vision V3.6](../architecture/NBSR_Protocol_Vision_V3.6.md)

**Runtime authorization:** None. This record reconciles documentation and
planning only.

## Approved V3.6 architecture

The following concepts are approved for forward-looking architecture and
test-plan work:

1. **Resolver-first:** every successful resolution through an upgraded NBSR
   network returns a scoped synthetic IP. Origin addresses remain internal and
   direct client fallback is forbidden.
2. **Identity separation:** Service Identity, Synthetic IP, Origin Endpoint,
   Source Edge, Destination Edge, and connector/service target are separate
   concepts. Service Identity is never an IP.
3. **Layering:** `Transport Session -> Route Context / Service Channel ->
   Application Stream`.
4. **Session reuse, service isolation:** compatible authenticated transport may
   be reused, but every Service Channel has independent authorization,
   service/name binding, revocation, quotas, audit attribution, failure
   containment, and security context.
5. **Cryptographic separation invariant:** every Service Channel has an
   independent cryptographic or transcript-bound context. No exact derivation
   algorithm is approved by this statement.
6. **Origin lifecycle:** the migration direction is `LEGACY_DNS`, `HYBRID`,
   then `NBSR_NATIVE`; legacy DNS is reachability only.
7. **Origin precedence:** valid signed native OriginSet, valid delegated
   Destination Edge/connector metadata, constrained legacy DNS, then fail
   closed.
8. **Safe replacement:** validate new endpoints before use, direct new channels
   to valid replacements, and drain or explicitly invalidate old endpoints.
9. **Mobility:** path changes do not create identity or authorization.
   Migration, resumption, failover, and handover preserve every existing
   security binding.
10. **Application-protocol boundary:** NBSR owns secure routing, not user login,
    OAuth, HTTP semantics, database semantics, or application transactions.

These are architectural decisions, not new Core v0.1 wire allocations.

## Frozen Core v0.1 decisions preserved

D1-D6 remain approved and unchanged. In particular:

- the complete mapping of 17 message codes remains unchanged;
- the complete mapping of 19 error codes remains unchanged;
- the six D6 numeric object schemas remain unchanged;
- deterministic CBOR and COSE wrapper rules remain unchanged;
- ServiceRecord, RouteGrant, and Revocation remain the only D6 object-level
  COSE Sign1 payloads;
- revocation `expires_at`, `target_sequence`, and issuer-generation tombstone
  behavior remain unchanged; and
- the frozen Resolution, Tunnel, Stream, and Connector state registries and
  transitions remain unchanged.

The V3.6 conceptual publication modes `LEGACY_DNS`, `HYBRID`, and
`NBSR_NATIVE` do not silently replace or alias the D6 ServiceRecord
`publication_mode` values `legacy`, `dual-published`, `nbsr-preferred`, and
`nbsr-secure-only`. Their exact relationship is a human decision gate.

## Superseded forward-looking decisions

| Historical statement | Superseding V3.6 decision | Impact |
|---|---|---|
| Vision V3 allowed a legacy name to return ordinary origin A/AAAA data to the client | Every successful upgraded-network resolution returns a synthetic IP | WP2 must convert legacy reachability into an internal temporary OriginSet and never expose it |
| Vision V3 terminology separated legacy resolution from route state | Every successful result creates or retrieves synthetic mapping; secure routing begins when the client connects | Documentation and tests use a single client-visible outcome |
| Earlier tunnel descriptions did not distinguish shared transport from service authorization | Session reuse, service isolation | WP3 must not block reuse; WP4 owns reusable multi-service channels |
| Earlier origin handling described connector or DNS reachability without a complete publication lifecycle | Three migration modes, precedence, rollback protection, and safe drain | WP2, WP3, WP4, and WP6 gain explicit origin work |
| Earlier mobility text focused on a tunnel state | Continuity must preserve channel-, gateway-, client-, service-, policy-, expiry-, revocation-, and replay-binding | Migration/resumption/handover require dedicated gates and conformance plans |

The historical documents remain available with their original evidence and
dates. Supersession applies only to forward-looking claims.

## Compatibility-impact matrix

| V3.6 addition | Classification now | Core v0.1 impact | Approval required before wire/runtime work |
|---|---|---|---|
| Service Channel model | Internal implementation model; wire form unresolved | No current numeric schema or state registry | Yes |
| Multi-service Transport Session | Editorial architecture plus future behavior | Existing codes may be insufficient; no authorization reuse allowed | Yes for runtime profile |
| Cryptographic channel separation | Architectural invariant | No approved key schedule or encoded binding | Yes |
| OriginSet | Internal implementation model | Not a D6 object and not embedded into ServiceRecord | Yes |
| Origin update message | Core v0.2 candidate | No code among the frozen 17 message codes | Yes |
| Migration/resumption messages | Core v0.2 candidate | Tunnel states exist, but no dedicated message allocation is frozen | Yes |
| Gateway handover | Core v0.2 candidate | Authority, proof, and target binding unresolved | Yes |
| New state-machine transitions | Documentation-only proposal | Frozen transitions cannot change silently | Yes |
| New registry entries | Incompatible with the current freeze unless versioned | Would change exact mappings | Yes |
| New critical or non-critical extensions | Backward-compatible extension only if approved and permitted by the target schema | D6 signed-object schemas are closed; ControlEnvelope alone permits non-critical extension keys | Yes |
| Universal synthetic resolution | Editorial clarification with no wire impact | Existing `LEGACY_RESOLVED` may describe internal discovery; response remains synthetic | No wire change |
| Legacy DNS-to-OriginSet conversion | Internal implementation model in WP2 | No native OriginSet serialization | WP2 design approval |
| Safe origin drain | Internal lifecycle behavior, then possible Core v0.2 profile | Existing drain states do not define endpoint-update wire behavior | Yes |

No backward-compatible extension is approved by this matrix. It identifies the
classification route that a future proposal may take. Any direct modification
of a frozen D1-D6 value is an **incompatible change** requiring explicit human
approval and a versioning decision.

## Conflicts requiring human resolution

1. **Publication-mode vocabulary:** map, version, or separate the three V3.6
   lifecycle modes from the four frozen D6 wire values.
2. **Service Channel registry:** decide whether existing route/stream messages
   can carry the model without ambiguity or whether Core v0.2 codes are needed.
3. **Origin publication state:** decide whether it is local-only in WP2/WP3 or
   a new normative state registry.
4. **OriginSet authority:** decide issuer classes, delegated authority,
   signature wrapper, trust context, emergency revocation, and transparency.
5. **Drain semantics:** decide maximum lifetime and the conditions that force
   immediate invalidation instead of graceful completion.

Until approved, these conflicts block the relevant runtime or wire task, not
the documentation architecture.

## Pending human decisions

Explicit approval is still required for:

1. exact wire representation of OriginSet;
2. OriginSet wrapper and signing authority;
3. message codes for origin update;
4. service-channel wire representation;
5. cryptographic channel derivation and cross-language vectors;
6. transport-session scope and reuse key;
7. route grant reuse versus one grant per channel;
8. migration and resume message types;
9. gateway handover authority and proof;
10. route behavior when legacy DNS is unavailable, including any bounded
    last-known-good use;
11. origin health-check trust model and poisoning resistance;
12. graceful-drain maximum lifetime;
13. Core v0.1 extension versus Core v0.2;
14. mapping between V3.6 publication modes and frozen D6 values;
15. same-sequence different-content OriginSet equivocation policy;
16. split-brain publisher precedence and recovery;
17. exact audit identity and privacy fields for shared transport; and
18. transport-session compromise response and blast-radius policy;
19. exact per-Service-Channel TLS exporter label, canonical context,
    directionality, output use, rekey behavior, and vectors;
20. production Synthetic IP prefix/allocation strategy;
21. UDP profile selection and framing;
22. IP profile selection, selectors, MTU, and fragmentation behavior;
23. Web PKI requirement level for LEGACY_DNS and HYBRID origin candidates;
24. cross-edge resume semantics;
25. production timeout, quota, cache, and resource values;
26. HA staleness limits and fail-closed read requirements; and
27. approved interception mechanisms and bypass evidence per platform.

No item in this list is implicitly approved.

## Next approval gate

The next recommended decision is the **OriginSet compatibility and authority
gate**. It must select internal-only, approved extension, or Core v0.2
placement; define issuer/wrapper/trust rules; and resolve the publication-mode
vocabulary before any OriginSet runtime or native message implementation.
